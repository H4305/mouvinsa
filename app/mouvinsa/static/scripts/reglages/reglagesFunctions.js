//#region Birthdate
jQuery(document).ready(function($) {
    $('#birthdate_button').on('click', function () {

        var $birthdateEditButton = $('#birthdate_button');
        var $birthdateInput = $('#birthdate');
        var $birthdateSpan = $('#date_span');
        var $birthdateCancelButton = $('#date_cancel_button');
        var $birthdateValidateButton = $('#date_validate_button');


        $birthdateEditButton.hide();
        $birthdateSpan.hide();
        $birthdateInput.show();
        $birthdateCancelButton.show();
        $birthdateValidateButton.show();
    });
});

jQuery(document).ready(function($) {
    $('#date_cancel_button').on('click', function () {

        var $birthdateEditButton = $('#birthdate_button');
        var $birthdateInput = $('#birthdate');
        var $birthdateSpan = $('#date_span');
        var $birthdateCancelButton = $('#date_cancel_button');
        var $birthdateValidateButton = $('#date_validate_button');


        $birthdateEditButton.show();
        $birthdateSpan.show();
        $birthdateInput.hide();
        $birthdateCancelButton.hide();
        $birthdateValidateButton.hide();
    });
});

jQuery(document).ready(function($) {
    $('#date_validate_button').on('click', function () {

        var $birthdateEditButton = $('#birthdate_button');
        var $birthdateSpan = $('#date_span');
        var $birthdateCancelButton = $('#date_cancel_button');
        var $birthdateValidateButton = $('#date_validate_button');


        $birthdateEditButton.show();
        $birthdateSpan.hide();
        $birthdateCancelButton.hide();
        $birthdateValidateButton.hide();
    });
});
//#endregion


//#region SEX
jQuery(document).ready(function($) {
    $('#sex_button').on('click', function () {

        var $sexEditButton = $('#sex_button');
        var $sexSelect = $('#sex');
        var $sexSpan = $('#sex_span');
        var $sexCancelButton = $('#sex_cancel_button');
        var $sexValidateButton = $('#sex_validate_button');

        $sexEditButton.hide();
        $sexSelect.show();
        $sexSpan.hide();
        $sexCancelButton.show();
        $sexValidateButton.show();
    });
});

jQuery(document).ready(function($) {
    $('#sex_cancel_button').on('click', function () {

        var $sexEditButton = $('#sex_button');
        var $sexSelect = $('#sex');
        var $sexSpan = $('#sex_span');
        var $sexCancelButton = $('#sex_cancel_button');
        var $sexValidateButton = $('#sex_validate_button');

        $sexEditButton.show();
        $sexSelect.hide();
        $sexSpan.show();
        $sexCancelButton.hide();
        $sexValidateButton.hide();
    });
});

jQuery(document).ready(function($) {
    $('#sex_validate_button').on('click', function () {

        var $sexEditButton = $('#sex_button');
        var $sexSpan = $('#sex_span');
        var $sexCancelButton = $('#sex_cancel_button');
        var $sexValidateButton = $('#sex_validate_button');

        $sexEditButton.show();
        $sexSpan.hide();
        $sexCancelButton.hide();
        $sexValidateButton.hide();
    });
});
//#endregion


//#region WEIGHT
jQuery(document).ready(function($) {
    $('#weight_button').on('click', function () {

        var $weightEditButton = $('#weight_button');
        var $weightInput = $('#weight');
        var $weightSpan = $('#weight_span');
        var $weightCancelButton = $('#weight_cancel_button');
        var $weightValidateButton = $('#weight_validate_button');

        $weightEditButton.hide();
        $weightInput.show();
        $weightSpan.hide();
        $weightCancelButton.show();
        $weightValidateButton.show();
    });
});

jQuery(document).ready(function($) {
    $('#weight_cancel_button').on('click', function () {

        var $weightEditButton = $('#weight_button');
        var $weightInput = $('#weight');
        var $weightSpan = $('#weight_span');
        var $weightCancelButton = $('#weight_cancel_button');
        var $weightValidateButton = $('#weight_validate_button');

        $weightEditButton.show();
        $weightInput.hide();
        $weightSpan.show();
        $weightCancelButton.hide();
        $weightValidateButton.hide();
    });
});

jQuery(document).ready(function($) {
    $('#weight_validate_button').on('click', function () {

        var $weightEditButton = $('#weight_button');
        var $weightSpan = $('#weight_span');
        var $weightCancelButton = $('#weight_cancel_button');
        var $weightValidateButton = $('#weight_validate_button');

        $weightEditButton.show();
        $weightSpan.hide();
        $weightCancelButton.hide();
        $weightValidateButton.hide();
    });
});
//#endregion


//#region HEIGHT
jQuery(document).ready(function($) {
    $('#height_button').on('click', function () {

        var $heightEditButton = $('#height_button');
        var $heightInput = $('#height');
        var $heightSpan = $('#height_span');
        var $heightCancelButton = $('#height_cancel_button');
        var $heightValidateButton = $('#height_validate_button');

        $heightEditButton.hide();
        $heightInput.show();
        $heightSpan.hide();
        $heightCancelButton.show();
        $heightValidateButton.show();
    });
});

jQuery(document).ready(function($) {
    $('#height_cancel_button').on('click', function () {

        var $heightEditButton = $('#height_button');
        var $heightInput = $('#height');
        var $heightSpan = $('#height_span');
        var $heightCancelButton = $('#height_cancel_button');
        var $heightValidateButton = $('#height_validate_button');

        $heightEditButton.show();
        $heightInput.hide();
        $heightSpan.show();
        $heightCancelButton.hide();
        $heightValidateButton.hide();
    });
});

jQuery(document).ready(function($) {
    $('#height_validate_button').on('click', function () {

        var $heightEditButton = $('#height_button');
        var $heightSpan = $('#height_span');
        var $heightCancelButton = $('#height_cancel_button');
        var $heightValidateButton = $('#height_validate_button');

        $heightEditButton.show();
        $heightSpan.hide();
        $heightCancelButton.hide();
        $heightValidateButton.hide();
    });
});
//#endregion


//#region CYCLE
jQuery(document).ready(function($) {
    $('#cycle_button').on('click', function () {

        var $cycleEditButton = $('#cycle_button');
        var $cycleSelect = $('#cycle');
        var $cycleSpan = $('#cycle_span');
        var $cycleCancelButton = $('#cycle_cancel_button');
        var $cycleValidateButton = $('#cycle_validate_button');

        $cycleEditButton.hide();
        $cycleSelect.show();
        $cycleSpan.hide();
        $cycleCancelButton.show();
        $cycleValidateButton.show();
    });
});

jQuery(document).ready(function($) {
    $('#cycle_cancel_button').on('click', function () {

        var $cycleEditButton = $('#cycle_button');
        var $cycleSelect = $('#cycle');
        var $cycleSpan = $('#cycle_span');
        var $cycleCancelButton = $('#cycle_cancel_button');
        var $cycleValidateButton = $('#cycle_validate_button');

        $cycleEditButton.show();
        $cycleSelect.hide();
        $cycleSpan.show();
        $cycleCancelButton.hide();
        $cycleValidateButton.hide();
    });
});

jQuery(document).ready(function($) {
    $('#cycle_validate_button').on('click', function () {

        var $cycleEditButton = $('#cycle_button');
        var $cycleSpan = $('#cycle_span');
        var $cycleCancelButton = $('#cycle_cancel_button');
        var $cycleValidateButton = $('#cycle_validate_button');

        $cycleEditButton.show();
        $cycleSpan.hide();
        $cycleCancelButton.hide();
        $cycleValidateButton.hide();
    });
});
//#endregion


//#region YEAR
jQuery(document).ready(function($) {
    $('#year_button').on('click', function () {

        var $yearEditButton = $('#year_button');
        var $yearSelect = $('#year');
        var $yearSpan = $('#annee_span');
        var $yearCancelButton = $('#year_cancel_button');
        var $yearValidateButton = $('#year_validate_button');

        $yearEditButton.hide();
        $yearSelect.show();
        $yearSpan.hide();
        $yearCancelButton.show();
        $yearValidateButton.show();
    });
});

jQuery(document).ready(function($) {
    $('#year_cancel_button').on('click', function () {

        var $yearEditButton = $('#year_button');
        var $yearSelect = $('#year');
        var $yearSpan = $('#annee_span');
        var $yearCancelButton = $('#year_cancel_button');
        var $yearValidateButton = $('#year_validate_button');

        $yearEditButton.show();
        $yearSelect.hide();
        $yearSpan.show();
        $yearCancelButton.hide();
        $yearValidateButton.hide();
    });
});

jQuery(document).ready(function($) {
    $('#year_validate_button').on('click', function () {

        var $yearEditButton = $('#year_button');
        var $yearSpan = $('#annee_span');
        var $yearCancelButton = $('#year_cancel_button');
        var $yearValidateButton = $('#year_validate_button');

        $yearEditButton.show();
        $yearSpan.hide();
        $yearCancelButton.hide();
        $yearValidateButton.hide();
    });
});
//#endregion

//#region BRANCH
jQuery(document).ready(function($) {
    $('#branch_button').on('click', function () {

        var $branchEditButton = $('#branch_button');
        var $branchSelect = $('#branch');
        var $branchSpan = $('#branch_span');
        var $branchCancelButton = $('#branch_cancel_button');
        var $branchValidateButton = $('#branch_validate_button');

        $branchEditButton.hide();
        $branchSelect.show();
        $branchSpan.hide();
        $branchCancelButton.show();
        $branchValidateButton.show();
    });
});

jQuery(document).ready(function($) {
    $('#branch_cancel_button').on('click', function () {

        var $branchEditButton = $('#branch_button');
        var $branchSelect = $('#branch');
        var $branchSpan = $('#branch_span');
        var $branchCancelButton = $('#branch_cancel_button');
        var $branchValidateButton = $('#branch_validate_button');

        $branchEditButton.show();
        $branchSelect.hide();
        $branchSpan.show();
        $branchCancelButton.hide();
        $branchValidateButton.hide();
    });
});

jQuery(document).ready(function($) {
    $('#branch_validate_button').on('click', function () {

        var $branchEditButton = $('#branch_button');
        var $branchSpan = $('#branch_span');
        var $branchCancelButton = $('#branch_cancel_button');
        var $branchValidateButton = $('#branch_validate_button');

        $branchEditButton.show();
        $branchSpan.hide();
        $branchCancelButton.hide();
        $branchValidateButton.hide();
    });
});
//#endregion


//#region NOM
jQuery(document).ready(function($) {
    $('#nom_button').on('click', function () {

        var $nomEditButton = $('#nom_button');
        var $nomInput = $('#lastname');
        var $nomSpan = $('#nom_span');
        var $nomCancelButton = $('#nom_cancel_button');
        var $nomValidateButton = $('#nom_validate_button');

        $nomEditButton.hide();
        $nomInput.show();
        $nomSpan.hide();
        $nomCancelButton.show();
        $nomValidateButton.show();
    });
});

jQuery(document).ready(function($) {
    $('#nom_cancel_button').on('click', function () {

        var $nomEditButton = $('#nom_button');
        var $nomInput = $('#lastname');
        var $nomSpan = $('#nom_span');
        var $nomCancelButton = $('#nom_cancel_button');
        var $nomValidateButton = $('#nom_validate_button');

        $nomEditButton.show();
        $nomInput.hide();
        $nomSpan.show();
        $nomCancelButton.hide();
        $nomValidateButton.hide();
    });
});

jQuery(document).ready(function($) {
    $('#nom_validate_button').on('click', function () {

        var $nomEditButton = $('#nom_button');
        var $nomSpan = $('#nom_span');
        var $nomCancelButton = $('#nom_cancel_button');
        var $nomValidateButton = $('#nom_validate_button');

        $nomEditButton.show();
        $nomSpan.hide();
        $nomCancelButton.hide();
        $nomValidateButton.hide();
    });
});
//#endregion


//#region PRENOM
jQuery(document).ready(function($) {
    $('#prenom_button').on('click', function () {

        var $prenomEditButton = $('#prenom_button');
        var $prenomSelect = $('#firstname');
        var $prenomSpan = $('#prenom_span');
        var $prenomCancelButton = $('#prenom_cancel_button');
        var $prenomValidateButton = $('#prenom_validate_button');

        $prenomEditButton.hide();
        $prenomSelect.show();
        $prenomSpan.hide();
        $prenomCancelButton.show();
        $prenomValidateButton.show();
    });
});

jQuery(document).ready(function($) {
    $('#prenom_cancel_button').on('click', function () {

        var $prenomEditButton = $('#prenom_button');
        var $prenomSelect = $('#firstname');
        var $prenomSpan = $('#prenom_span');
        var $prenomCancelButton = $('#prenom_cancel_button');
        var $prenomValidateButton = $('#prenom_validate_button');

        $prenomEditButton.show();
        $prenomSelect.hide();
        $prenomSpan.show();
        $prenomCancelButton.hide();
        $prenomValidateButton.hide();
    });
});

jQuery(document).ready(function($) {
    $('#prenom_validate_button').on('click', function () {

        var $prenomEditButton = $('#prenom_button');
        var $prenomSpan = $('#prenom_span');
        var $prenomCancelButton = $('#prenom_cancel_button');
        var $prenomValidateButton = $('#prenom_validate_button');

        $prenomEditButton.show();
        $prenomSpan.hide();
        $prenomCancelButton.hide();
        $prenomValidateButton.hide();
    });
});
//#endregion


//#region PASSWORD
jQuery(document).ready(function($) {
    $('#password_button').on('click', function () {

        var $passwordEditButton = $('#password_button');
        var $passwordSelect = $('#password');
        var $passwordSpan = $('#password_span');
        var $passwordCancelButton = $('#password_cancel_button');
        var $passwordValidateButton = $('#password_validate_button');

        $passwordEditButton.hide();
        $passwordSelect.show();
        $passwordSpan.hide();
        $passwordCancelButton.show();
        $passwordValidateButton.show();
    });
});

jQuery(document).ready(function($) {
    $('#password_cancel_button').on('click', function () {

        var $passwordEditButton = $('#password_button');
        var $passwordSelect = $('#password');
        var $passwordSpan = $('#password_span');
        var $passwordCancelButton = $('#password_cancel_button');
        var $passwordValidateButton = $('#password_validate_button');

        $passwordEditButton.show();
        $passwordSelect.hide();
        $passwordSpan.show();
        $passwordCancelButton.hide();
        $passwordValidateButton.hide();
    });
});

jQuery(document).ready(function($) {
    $('#password_validate_button').on('click', function () {

        var $passwordEditButton = $('#password_button');
        var $passwordSpan = $('#password_span');
        var $passwordCancelButton = $('#password_cancel_button');
        var $passwordValidateButton = $('#password_validate_button');

        $passwordEditButton.show();
        $passwordSpan.hide();
        $passwordCancelButton.hide();
        $passwordValidateButton.hide();
    });
});
//#endregion


//#region GOAL
jQuery(document).ready(function($) {
    $('#fitness_button').on('click', function () {

        var $fitnessEditButton = $('#fitness_button');
        var $fitnessInput = $('#goal');
        var $fitnessSpan = $('#fitness_span');
        var $fitnessCancelButton = $('#fitness_cancel_button');
        var $fitnessValidateButton = $('#fitness_validate_button');

        $fitnessEditButton.hide();
        $fitnessInput.show();
        $fitnessSpan.hide();
        $fitnessCancelButton.show();
        $fitnessValidateButton.show();
    });
});

jQuery(document).ready(function($) {
    $('#fitness_cancel_button').on('click', function () {

        var $fitnessEditButton = $('#fitness_button');
        var $fitnessInput = $('#goal');
        var $fitnessSpan = $('#fitness_span');
        var $fitnessCancelButton = $('#fitness_cancel_button');
        var $fitnessValidateButton = $('#fitness_validate_button');

        $fitnessEditButton.show();
        $fitnessInput.hide();
        $fitnessSpan.show();
        $fitnessCancelButton.hide();
        $fitnessValidateButton.hide();
    });
});

jQuery(document).ready(function($) {
    $('#fitness_validate_button').on('click', function () {

        var $fitnessEditButton = $('#fitness_button');
        var $fitnessSpan = $('#fitness_span');
        var $fitnessCancelButton = $('#fitness_cancel_button');
        var $fitnessValidateButton = $('#fitness_validate_button');

        $fitnessEditButton.show();
        $fitnessSpan.hide();
        $fitnessCancelButton.hide();
        $fitnessValidateButton.hide();
    });
});
//#endregion









